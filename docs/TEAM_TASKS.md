# Team Tasks

1. Kiểm tra lại contract Lab 02 trong `contracts/openapi.yml`.
2. Chạy `npm run lint`.
3. Chạy `npm run mock:iot`.
4. Chạy thêm `npm run mock:vision` để phục vụ consumer-side smoke test.
5. Chạy `npm run test:mock`.
6. Nếu service thật đã có, chạy `npm run test:local`.
7. Nộp toàn bộ repo kèm report trong `reports/`.
